<div class="page-header">
  <h1>
    <%= detail_list_title @category, @organization, @month %> 
  </h1>
</div>

<table class="table table-striped table-bordered">
  <tr>
    <th><%= Reimbursement.human_attribute_name(:sn) %></th>
    <th><%= Reimbursement.human_attribute_name(:reimburse_on) %></th>
    <th><%= Reimbursement.human_attribute_name(:organization) %></th>
    <th><%= Detail.human_attribute_name(:category) %></th>
    <th><%= Detail.human_attribute_name(:name) %></th>
    <th><%= Detail.human_attribute_name(:amount) %></th>
    <th><%= Detail.human_attribute_name(:unit) %></th>
    <th><%= Detail.human_attribute_name(:unit_price) %></th>
    <th><%= Detail.human_attribute_name(:price) %></th>
  </tr>
  <% for detail in @details %>
    <tr>
      <td><%= detail.reimbursement.sn %></td>
      <td><%= detail.reimbursement.reimburse_on %></td>
      <td><%= detail.reimbursement.organization.name %></td>
      <td><%= detail.category.name %></td>
      <td><%= detail.name %></td>
      <td><%= detail.amount %></td>
      <td><%= detail.unit %></td>
      <td><%= detail.unit_price %></td>
      <td><%= detail.price %></td>
    </tr>
  <% end %>
</table>
